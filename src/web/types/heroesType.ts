export type heroesType = {
  id: string;
  superhero: string;
  publisher: string;
  alter_ego: string;
  first_appearance: string;
  characters: string;
};
export type PagePublisherProps = {
  publisher: string;
};
export type HeroeItemType = {
  heroe: heroesType;
};
